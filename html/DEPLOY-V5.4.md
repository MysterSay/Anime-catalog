# Yoru v5.4

Виправлення Cloudflare Error 1102 на `/api/anime`:
- компактний JSON без pretty-print;
- каталог отримує легку summary-модель без усіх посилань/банера/аліасів;
- сторінка конкретного тайтлу читається напряму через `/pages/{id}`, а не через повне сканування бази.
