# Yoru v5.8

- Notion number property `Переглянуто` with +1 button on title page.
- Settings button on the catalog.
- Group name/color editor backed by Notion select options.
- Group colors exposed through `/api/options`, `/api/anime`, and `/api/extension/context`.

Deploy:

```powershell
npx --yes wrangler@4.120.0 pages deploy . --project-name myster-anime
```
